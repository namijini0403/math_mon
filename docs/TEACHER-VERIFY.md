# 선생님이 직접 검증해야 할 항목 (콘텐츠 품질 — Phase B 풀이 정비)

> 제(Claude)가 코드·표기 가드·수학 논리로 검증할 수 있는 건 다 했습니다.
> 아래는 **사람의 판단·눈·교실 맥락이 필요해서 제가 단정할 수 없는 것들**만 모았습니다.
> 행정·계정·구매는 [TEACHER-TODO.md](TEACHER-TODO.md), 누적 교육과정 기준은 [curriculum/g{N}.md](curriculum/), 자동 검증되는 표기는 game/curriculumGuard.ts가 강제합니다.

---

## ▶ 지금 선생님이 봐 주실 것 (남은 항목)

- [ ] **부등호(<, >)의 '말' 처리** — 선생님이 직접 확인하기로 하신 항목. 앱 정책 = 부등호 입력은 클릭 선택, 풀이는 '말 + 기호'. 1·2학년은 기호 대신 '말'만. 단원별로 자연스러운지 눈으로 확인.
- [ ] **1·2학년 '말로 된' 연산 표본 점검** — 가드는 `÷`·`×` 기호만 잡고, "○로 나누면"처럼 **말로 쓴 나눗셈/곱셈**은 못 잡습니다. 발견한 "나누면"류는 제가 "가르면"으로 바꿨고(아래 ✅), 새 시드가 무한이라 표본으로 더 보시면 좋습니다. 더 나오면 스킬 id 주시면 고칩니다.
- [ ] **그림이 필요한 문제** — 자동 SVG 그림 시스템을 만드는 중입니다(아래 ▶진행 중). 적용되면 화면 캡처로 그림이 문제와 맞는지 봐 주세요.

## ▶ 진행 중 (제가 작업)

- **그림 자동 렌더(SVG) 시스템**: 도형·종이접기 등 그림이 필요한 유형을 코드로 간단히 그려 넣는 기능. 설계안은 docs/figure-system-plan.md. 1차로 합동/대칭·종이접기·계단도형·겹친도형·정육면체 칠하기 등 심화 유형부터 그림을 붙입니다. (앱 철학 = 이미지 대신 코드 렌더 SVG, 배지/메달과 동일.)

## ✅ 이번에 해결한 것 (검증 불필요, 기록용)

> 선생님이 지난번 짚어 주신 것 + 제가 audit·실제 화면 검수로 잡은 것들입니다.

- **ch51-gcdlcm 답 유일성**: 문제에 "**두 자리 수인 두 자연수**" 제약을 넣어 답을 유일하게 만들었습니다. (이전엔 (G, G×p×q)도 답이 될 수 있었음 → 이제 그 쌍은 큰 수가 세 자리라 자동 제외, 풀이에도 그 이유를 설명.) 실제 화면으로 확인 완료.
- **해설의 문자 미지수 제거**: ch51-age·arith의 `x`·`n`, ch51-frac-range·telescope의 `k` → 전부 `□` 기호로. (심화 문제 자체의 미지수 기호 ㉮·㉯·○·△·□는 그대로 둠 — 선생님 OK.)
- **풀이 텍스트 중복 버그(중대)**: 심화 풀이에서 `…= 99예요예요`처럼 **값과 어미가 두 번씩 찍히던 버그**(ida 함수 오용)를 심화 전 스킬 28곳에서 수정. 실제 화면으로 확인 완료.
- **거듭제곱·부동소수점**: ch52-paintcut `(n−2)²` → `(n−2)×(n−2)` + 가드가 `)²`도 잡도록 강화. ch51-ineq·arith·ch52-fracineqnat의 긴 소수 노출 제거.
- **1·2학년 '나누면' → '가르면'**: g2 세 자리/네 자리 수(자릿값 가르기), g1 뺄셈 문장제 표현을 학년에 맞게 교체.
- **화면 시각 검수(Playwright 재연결됨)**: 5학년 분수 덧셈·심화 문제에서 💡풀이 펼침, **분수·대분수·㉮㉯·□ 렌더 모두 정상** 확인.

## ✅ 난이도 — 선생님 OK
- 심화가 상당히 높은 수준(등차수열·망원 소거·정육면체 칠하기 등)이어도, **상위권 추가학습용**이라 괜찮다고 확인해 주심. 별도 조치 없음.

---

## 검증 결과 회신 방법
- 고칠 게 보이면: 스킬 id(예: `ch52-congtriangle`)와 함께 알려 주시면 제가 코드를 고칩니다.
- 그림이 문제와 안 맞으면: 어느 유형인지 알려 주세요.
