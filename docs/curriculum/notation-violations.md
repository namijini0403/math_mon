# 풀이 표기 위반 추적 (curriculumGuard가 발견) — 순차 정비 대기 목록

> 2026-06-14 가드 1차 스캔(스킬당 25시드)에서 발견된 **학년 부적합 표기**. 단원 audit 시 고치고, 고치면 줄에 ✅.
> 가드(client/src/game/curriculumGuard.ts)는 AUDITED_UNITS에 등록된 단원만 실패로 강제 — 아래는 미등록(=아직 도달 전) 단원의 알려진 위반.

## 1학년 (× 곱셈기호 — 곱셈은 2학년)
- [x] `num50-compose` (1-1 50까지의 수): ✅ "10개씩 N묶음(=N0개) + 낱개 M개" 말로 수정(AUDITED).
- [x] `num100-compose` (1-2 100까지의 수): ✅ 묶음 말로 수정(AUDITED).
- [x] `num100-evenodd` (1-2): ✅ 가드 밖 수동 적발 — "2로 나누면 나머지"(나눗셈=3학년) → "둘씩 짝지으면 남는 것" 말로 수정.

## 2학년 — ✅ 전 단원 audit 완료 (2-1·2-2 모두 AUDITED)
- [x] `muli-times` (2-1 곱셈): ✅ `÷` 제거 — "a씩 뛰어 세기"로 몇 배 설명.
- [x] 2-1 세 자리 수·도형: ✅ 자릿값 `×100`·`perShape×count` 곱셈기호 제거 → 자릿값 덧셈·반복 덧셈(곱셈은 2-1 마지막 단원).
- [x] `gugu-missing` (2-2 곱셈구구): ✅ `÷` 제거 — "N단을 외워 N×?=결과" 식으로.
- [x] `len22-conv` (2-2 길이 재기): ✅ `÷100`·`×100` 제거 → "100 cm가 1 m" 묶음 말로.
- [x] `time2-conv`, `time2-units` (2-2 시각과 시간): ✅ `÷60`·`÷24`·`×60`·`×24` 제거 → "60분이 1시간"·"24시간이 하루" 묶음 말로. `time2-read`의 `11×5`(구구 범위 밖)도 "5분씩 N칸"으로.
- [x] `rule2-shape`, `rule2-word` (2-2 규칙 찾기): ✅ `÷`+몫/나머지(3학년) 제거 → "반복되는 N개 중 K번째와 같다"로.
- [x] `rule2-stack` (2-2 규칙 찾기): ✅ `2²·3²`(거듭제곱=전 학년 금지) 제거 → "1×1, 2×2, 3×3…"로.

> 2학년 곱셈(×) 사용 원칙: 곱셈구구(9×9) 범위 안, 곱셈표·곱셈 단원에서만. 자릿값·단위 변환은 덧셈/묶음 말(곱셈 미도입 단원 배려 + 구구 범위 밖 곱 회피). 상세 docs/curriculum/g2.md.

## 6학년 (숫자 거듭제곱 2² — 지수는 중등, 초등은 `n × n`)
- [ ] `ch61-cube-edge` (6-1 심화): `n²` → `n × n`.
- [ ] `ch62-circle-biggest`, `ch62-compound-area`, `ch62-cylinder-net`, `ch62-quarter-cut`, `ch62-rotate-cross-diff` (6-2 심화): 원 넓이를 `r² × 3.14` → `r × r × 3.14`(초등 원의 넓이 = 반지름 × 반지름 × 원주율).

> 참고: 정식(비심화) 6학년 원의 넓이 단원(unitCircle)도 도달 시 같은 `r × r × 원주율` 표기 확인.
> 부등호(<,>)는 앱 결정상 사용 OK(입력은 클릭 선택만) — 가드 대상 아님(docs/curriculum/g1.md 참조).
